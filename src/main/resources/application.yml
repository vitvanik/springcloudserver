server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vitvanik/config
          repos:
            local:
              pattern: '*/local'
              uri: file:///${user.home}/config-repo